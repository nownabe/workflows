[tools]
"aqua:rhysd/actionlint" = "1.7.12"
"aqua:suzuki-shunsuke/ghalint" = "1.5.6"
# Temporarily aligned with the zizmor TOOL pin in the released github-workflow.yaml
# (dogfooding via _pr.yaml runs the released workflow, which installs with --locked
# against this repo's mise.lock). Safe to bump again once a release with the
# `install: false` fix is out and _pr.yaml points to it.
"aqua:zizmorcore/zizmor" = "1.27.0"
"aqua:betterleaks/betterleaks" = "1.6.1"
"aqua:trufflesecurity/trufflehog" = "3.95.9"
"aqua:nodejs/node" = "24.18.0" # runtime for the npm-backed tools below
# aqua has native binaries (oxc apps_v) but our pinned mise's baked registry can't resolve them yet, so use npm
"npm:oxlint" = "1.75.0"
"npm:oxfmt" = "0.60.0"

[settings]
lockfile = true
locked = true
minimum_release_age = "3d"
